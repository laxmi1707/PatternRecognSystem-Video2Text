# Base URL of the backend API (e.g. http://localhost:8000).
# Leave unset/commented out to use the built-in mock analysis pipeline instead -
# that's the default, and it needs no backend running at all.
# VITE_API_BASE_URL=http://localhost:8000
